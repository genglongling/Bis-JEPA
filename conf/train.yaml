defaults:
  - _self_
  - env: pusht
  - encoder: dino
  - action_encoder: proprio
  - proprio_encoder: proprio
  - decoder: vqvae
  - predictor: vit
  - override hydra/launcher: submitit_slurm

# base path to save model outputs. Checkpoints will be saved to ${ckpt_base_path}/outputs.
ckpt_base_path: ./ # put absolute path here

# If set (non-null, non-empty), overrides the default wandb.run.name built from Hydra dirname + env/skip/hist keys.
wandb_display_name: null

hydra:
  run:
    dir: ${ckpt_base_path}/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
  sweep:
    dir: ${ckpt_base_path}/outputs/${now:%Y-%m-%d}/${now:%H-%M-%S}
    subdir: ${hydra.job.num}
  launcher:
    submitit_folder: ${hydra.sweep.dir}/.submitit/%j
    nodes: 1
    tasks_per_node: 1
    cpus_per_task: 8
    mem_gb: 512
    gres: "gpu:h100:1"
    timeout_min: 2880
    setup: ["export DEBUGVAR=$(scontrol show hostnames $SLURM_JOB_NODELIST)",
            export MASTER_ADDR="$(scontrol show hostnames $SLURM_JOB_NODELIST | head -n 1)",
            "export MASTER_PORT=$(for port in $(shuf -i 30000-65500 -n 20); do if [[ $(netstat -tupln 2>&1 | grep $port | wc -l) -eq 0 ]] ; then echo $port; break; fi; done;)",]

training:
  seed: 0
  epochs: 50  # Table 2
  batch_size: 20  # Table 2
  save_every_x_epoch: 1
  reconstruct_every_x_batch: 500
  num_reconstruct_samples: 6
  # Grids: save to <train|valid>/*.png; optional W&B media (scalars still always logged)
  log_image_mosaics_to_wandb: true
  # When has_decoder: false, still log ground-truth frames (verifies data + context length)
  log_ground_truth_mosaic: true
  encoder_lr: 5e-7
  decoder_lr: 3e-4  # Table 2
  predictor_lr: 1e-5  # Table 2
  action_encoder_lr: 1e-4  # Table 2 (proprio + action encoders)
  bisim_lr: 5e-7  # Table 2

img_size: 224 # 224 for point_maze and wall
frameskip: 5
concat_dim: 1

normalize_action: True

# action encoder
action_emb_dim: 10
num_action_repeat: 1

# proprio encoder
proprio_emb_dim: 10
num_proprio_repeat: 1

# bisimulation model settings (Table 3 + Figure 6)
has_bisim: True
bisim_latent_dim: 32  # latent / patch dim (Table 3)
bisim_hidden_dim: 256  # MLP hidden width (Figure 6; not in Table 3)
bisim_pos_encoding: grid_mlp  # grid_mlp (Figure 6), learned (legacy ckpts), or sincos
bisim_pos_hidden_dim: 256  # Positional Embedding Encoder hidden width
bisim_coef: 1
# When has_bisim: also train a DINO-WM JEPA head on frozen 384-d DINO patch tokens
train_dinowm_aux: True
dinowm_coef: 1.0
train_dinowm_predictor: True
var_loss_coef: 1
PCA1_loss_target: 0.0001
VC_target: 0.5  # lowered from 1.0 — DINOv2 natural variance ~0.3, old target was unreachable
num_pcs: 10
PCAloss_epoch: 50
bisim_memory_buffer_size: 1000  # Table 3
bisim_comparison_size: 200  # Table 3 (state comparison size)
# num_patches: 196 = (img_size/16)^2; patch_emb_dim: 384 from DINOv2 ViT-S/14 (Table 3)
# Bisim metric: predictor = gamma||T(w,a)-T(w',a')||^2 (paper); encoder_next = encoded o_{t+1}
bisim_transition_target: predictor
bisim_latent_metric: l2  # l2 (paper) or smooth_l1 (legacy)

# Bisim extra regularization: "pca" (default, hinge/PCA var + per-patch cov) or "vicreg"
regularization: pca
vicreg_inv_coef: 25.0
vicreg_var_coef: 25.0
vicreg_cov_coef: 1.0
vicreg_std_min: 1.0
# Weak-SIGReg (covariance-to-I) sketch size when regularization=sigreg
sigreg_sketch_dim: 64

num_hist: 3
num_pred: 1 # only supports 1
has_predictor: True # set this to False for only training a decoder
has_decoder: False # set this to False for only training a predictor

accelerate_launch: False

model:
  _target_: models.visual_world_model.VWorldModel
  image_size: ${img_size}
  num_hist: ${num_hist}
  num_pred: ${num_pred}
  train_encoder: False
  train_predictor: True
  train_decoder: True
  train_bisim: True  # Control whether to train the bisimulation model
  bypass_dinov2: False  # If True, train obs -> bisim directly, bypassing DinoV2

  train_w_std_loss: True
  train_w_reward_loss: False

debug: False 

# Planning params for planning eval jobs launched during training
plan_settings: 
  # plan_cfg_path: conf/plan.yaml # set to null for no planning evals
  plan_cfg_path: null 
  planner: ['gd', 'cem']
  goal_source: ['dset', 'random_state']
  goal_H: [5]
  alpha: [0.1, 1]
